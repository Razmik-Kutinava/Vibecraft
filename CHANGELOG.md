# Changelog

## [2026-04-19] — Фавикон, мобильные гармошки, SEO-разметка

### Фавикон
- Заменён `public/favicon.png`: был 2 MB, 1536×1024px, белый фон → стал 92 KB, 512×512px, тёмный фон `#141414`, логотип VC крупно
- Исправлены все ссылки с несуществующего `favicon.svg` на `favicon.png` (Layout.astro, site.ts, schema.ts, index.astro)
- Убран лишний `<link rel="icon">` из тела страницы `[lang]/index.astro` — был внутри `<body>` вместо `<head>`

### SEO / Schema.org
- Добавлен `BreadcrumbList` JSON-LD на все страницы кроме главной: `/blog`, `/blog/[slug]`, `/cases`, `/manifest`
- Исправлен `defaultOgImage` в `site.ts` и логотипы в `schema.ts` — везде теперь `.png`

### Мобильная адаптация (LandingPage.astro + landing.css)
- **Как работаем быстрее** — гармошка на мобилке: виден только заголовок, контент раскрывается по клику
- **Почему Vibecraft** — на мобилке показываются 3 карточки, остальные скрыты под кнопкой «Показать ещё»; 3 последних пункта скрыты полностью
- **Услуги** — карточки-гармошки: видно название + цена, описание и CTA раскрываются по клику (шеврон)
- **Процесс** — каждый шаг сворачивается на мобилке, виден только номер + заголовок
- **Манифест** (тизер) — на мобилке виден только первый абзац, остальное под «Читать далее»
- **Блог** — на мобилке видна одна статья, остальные под «Все статьи»
- На десктопе все блоки отображаются полностью без изменений

### WebGL
- Добавлен обработчик `webglcontextrestored` в HeroScene — кристалл восстанавливается автоматически если браузер убил WebGL-контекст
